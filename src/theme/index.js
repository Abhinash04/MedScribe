import colors from './colors';
import spacing from './spacing';
import typography from './typography';
import { BASE_WIDTH, scaleFor } from './scale';

export { colors, spacing, typography, BASE_WIDTH, scaleFor };
export default {
  colors,
  spacing,
  typography,
};
